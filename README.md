## Simple Weather app

A really simple weather app which displays temperature of a place and changes background color according to predefined temperature ranges.
Build using react

### steps to run app locally
use command npm install to install all dependencies and replace api in src/API_KEY.js with your own api as mentioned in comments

# Link for the live app 
## https://kingshukrox.github.io/weather-react/
